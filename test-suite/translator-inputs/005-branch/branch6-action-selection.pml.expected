bool a;
bool b;
bool c;
bool d;
bool e;

active proctype test()
{
	int br1 = _nr_pr;
	run act1();
	run sel1();
	_nr_pr == br1 ->
}

proctype act1()
{
	a ->
		e = true;
}

proctype sel1()
{
    if
    :: true ->
             a ->
                b = true;
    :: true ->
             b ->
                e = true;
    :: true ->
             c ->
                d = true;
    fi
}
